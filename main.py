import countries_inserts as country
import customers_inserts as customers
import employees as employees
import inventories_inserts as inventories
import locations_inserts as locations
import order_items_inserts as order_items
import orders_inserts as orders
import products_inserts as products
import warehouses_inserts as warehouses


def main():
    # country.generate()
    # customers.generate()
    # employees.generate()
    inventories.generate()
    # locations.generate()
    order_items.generate()
    # orders.generate()
    products.generate()
    # warehouses.generate()


main()

